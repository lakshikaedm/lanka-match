<h1>Browse Profiles</h1>

<% if @profiles.any? %>
  <% @profiles.each do |profile| %>
    <div>
      <h2><%= link_to profile.display_name, public_profile_path(profile) %></h2>
      <p>Age: <%= profile.age %></p>
      <p>Gender: <%= profile.gender.humanize %></p>
      <p>Location: <%= profile.location %></p>
      <p>Occupation: <%= profile.occupation %></p>
      <p><%= truncate(profile.bio, length: 100, separator: ' ' ) %></p>
    </div>
    <% if user_signed_in? && current_user != profile.user %>
      <% if current_user.given_likes.exists?(liked: profile.user) %>
        <%= button_tag "Liked", disabled: true %>
      <% else %>
        <%= button_to "Like",
            public_profile_like_path(profile),
            method: :post %>
      <% end %>
    <% end %>
    <hr>
  <% end %>
<% else %>
  <p>No profiles available yet.</p>
<% end %>
